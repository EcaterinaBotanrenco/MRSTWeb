@model WebApplication1.Web.Models.UserLogin

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style=" padding-left: 800px;" class="container-fluid d-none d-lg-block">
    <div class="container-center">
        <div class="view-header">
            <div class="header-icon">
                <i class="pe page-header-icon pe-7s-unlock"></i>
            </div>
            <div class="header-title">
                <h3>Login</h3>
            </div>
        </div>

        <div class="panel panel-filled">
            <div class="panel-body">
                @Html.ValidationSummary(true)
                @using (Html.BeginForm("Login", "Login", FormMethod.Post, new { id = "LoginForm" }))
                {
                    @Html.AntiForgeryToken()

                    <div style=" width: 300px;" class="form-group">
                        @Html.LabelFor(m => m.Credential, new { @class = "control-label" })
                        @Html.TextBoxFor(m => m.Credential, new
                        {
                            placeholder = "example@mail.com",
                            title = "Please enter your username",
                            @class = "form-control"
                        })
                    </div>
                    <div style=" width: 300px;" class="form-group">
                        @Html.LabelFor(m => m.Password, new { @class = "control-label" })
                        @Html.PasswordFor(m => m.Password, new
                        {
                            @class = "form-control",
                            placeholder = "password",
                            title = "Please enter your password"
                        })
                    </div>
                    <div>
                        <button style=" background-color: #F9A392; color: white;" type="submit" class="btn ">Login</button>
                    </div>
                }
                <a href="@Url.Action("Register", "Register")" class="small">Create an account</a>
            </div>
        </div>
    </div>
</div>